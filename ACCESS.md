# 🌐 Быстрый доступ к сайту извне

## 📋 Быстрая инструкция

### 1. Запустите Docker контейнер

```bash
docker-compose up -d --build
```

### 2. Узнайте ваш IP адрес

**macOS:**
```bash
ipconfig getifaddr en0
# Или для WiFi:
ipconfig getifaddr en1
```

**Linux:**
```bash
hostname -I | awk '{print $1}'
# Или:
ip addr show | grep "inet " | grep -v 127.0.0.1
```

**Windows:**
```cmd
ipconfig
# Найдите IPv4 адрес (например, 192.168.1.100)
```

### 3. Откройте сайт

**На вашем компьютере:**
- `http://localhost:3000`

**На другом устройстве в той же сети:**
- `http://ВАШ_IP_АДРЕС:3000`
- Например: `http://192.168.1.100:3000`

---

## 🔥 Настройка Firewall

### macOS

```bash
# Проверить статус
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate

# Если firewall включен, разрешить Node.js
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/local/bin/node
```

### Linux (ufw)

```bash
sudo ufw allow 3000/tcp
sudo ufw status
```

### Windows

1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Создайте правило для входящих подключений на порт 3000

---

## ✅ Проверка доступности

### С вашего компьютера:
```bash
curl http://localhost:3000
```

### С другого компьютера в сети:
```bash
curl http://ВАШ_IP_АДРЕС:3000
```

### В браузере на другом устройстве:
Просто откройте: `http://ВАШ_IP_АДРЕС:3000`

---

## 🔧 Изменение порта

Если порт 3000 занят, измените в `docker-compose.yml`:

```yaml
ports:
  - "0.0.0.0:8080:3000"  # Внешний порт:внутренний порт
```

Тогда доступ будет: `http://ВАШ_IP:8080`

---

## 🌍 Доступ через интернет (для людей не в вашей сети)

Есть несколько способов дать доступ к сайту через интернет:

---

### Вариант 1: Ngrok (самый простой для тестирования) ⭐ РЕКОМЕНДУЕТСЯ

Ngrok создает безопасный туннель к вашему локальному серверу.

#### 1. Установите Ngrok

**macOS:**
```bash
brew install ngrok/ngrok/ngrok
```

**Или скачайте с:** https://ngrok.com/download

#### 2. Запустите Docker контейнер

```bash
docker-compose up -d
```

#### 3. Создайте туннель

```bash
ngrok http 3000
```

#### 4. Получите публичный URL

Ngrok покажет что-то вроде:
```
Forwarding: https://abc123.ngrok-free.app -> http://localhost:3000
```

**Другой человек может открыть:** `https://abc123.ngrok-free.app`

#### 5. Для постоянного URL (платно)

- Зарегистрируйтесь на https://ngrok.com
- Получите authtoken
- Настройте постоянный домен

---

### Вариант 2: Port Forwarding на роутере

Если у вас есть публичный IP адрес и доступ к роутеру:

#### 1. Узнайте ваш публичный IP

```bash
curl ifconfig.me
# Или откройте: https://whatismyipaddress.com
```

#### 2. Настройте Port Forwarding на роутере

1. Войдите в админ-панель роутера (обычно `192.168.1.1` или `192.168.0.1`)
2. Найдите раздел "Port Forwarding" или "Виртуальные серверы"
3. Добавьте правило:
   - **Внешний порт:** 3000 (или другой, например 8080)
   - **Внутренний IP:** IP вашего компьютера (узнайте через `ipconfig getifaddr en0`)
   - **Внутренний порт:** 3000
   - **Протокол:** TCP

#### 3. Откройте порт в Firewall

**macOS:**
```bash
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/local/bin/node
```

**Linux:**
```bash
sudo ufw allow 3000/tcp
```

#### 4. Другой человек открывает

```
http://ВАШ_ПУБЛИЧНЫЙ_IP:3000
```

⚠️ **Внимание:** Открытие портов в интернет требует настройки безопасности!

---

### Вариант 3: Cloudflare Tunnel (бесплатно, безопасно)

Cloudflare Tunnel создает безопасное соединение без открытия портов.

#### 1. Установите cloudflared

**macOS:**
```bash
brew install cloudflare/cloudflare/cloudflared
```

**Или скачайте:** https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/

#### 2. Запустите туннель

```bash
cloudflared tunnel --url http://localhost:3000
```

#### 3. Получите URL

Cloudflare покажет URL вида:
```
https://random-name.trycloudflare.com
```

**Другой человек может открыть:** `https://random-name.trycloudflare.com`

---

### Вариант 4: Serveo (без установки)

Простой SSH туннель без установки дополнительного ПО.

#### 1. Запустите туннель

```bash
ssh -R 80:localhost:3000 serveo.net
```

#### 2. Получите URL

Serveo покажет URL вида:
```
https://random-name.serveo.net
```

**Другой человек может открыть:** `https://random-name.serveo.net`

---

### Вариант 5: Деплой на VPS/Cloud сервер

Для постоянного доступа лучше развернуть на облачном сервере:

1. **VPS** (DigitalOcean, Linode, Hetzner)
2. **Облачные платформы** (Vercel, Netlify, Railway)
3. **Docker на сервере** - скопируйте проект и запустите там

---

## 🎯 Рекомендации

### Для быстрого тестирования:
✅ **Ngrok** - самый простой способ

### Для постоянного доступа:
✅ **Cloudflare Tunnel** - бесплатно и безопасно
✅ **VPS/Cloud сервер** - для production

### Для временного доступа:
✅ **Serveo** - без установки, через SSH

---

## 🔒 Безопасность

⚠️ **Важно при открытии портов в интернет:**

1. Используйте HTTPS (настройте SSL сертификат)
2. Ограничьте доступ по IP (если возможно)
3. Используйте сильные пароли
4. Регулярно обновляйте зависимости
5. Мониторьте логи на подозрительную активность

---

## 📝 Сравнение вариантов

| Вариант | Сложность | Стоимость | Безопасность | Постоянный URL |
|---------|-----------|-----------|--------------|----------------|
| Ngrok | ⭐ Легко | Бесплатно/Платно | ✅ Хорошо | ❌/✅ |
| Port Forwarding | ⭐⭐ Средне | Бесплатно | ⚠️ Требует настройки | ✅ |
| Cloudflare Tunnel | ⭐⭐ Средне | Бесплатно | ✅ Отлично | ✅ |
| Serveo | ⭐ Легко | Бесплатно | ⚠️ Базово | ❌ |
| VPS/Cloud | ⭐⭐⭐ Сложно | Платно | ✅ Отлично | ✅ |

---

## 📱 Доступ с мобильного устройства

1. Убедитесь, что мобильное устройство в той же WiFi сети
2. Узнайте IP адрес компьютера (см. выше)
3. Откройте в браузере мобильного: `http://ВАШ_IP:3000`

---

## 🐛 Troubleshooting

### Проблема: Не могу подключиться с другого устройства

**Решение:**
1. Проверьте, что контейнер запущен: `docker-compose ps`
2. Проверьте firewall настройки
3. Убедитесь, что оба устройства в одной сети
4. Проверьте IP адрес: `ipconfig getifaddr en0` (macOS)

### Проблема: Порт занят

**Решение:**
```bash
# Проверить, что использует порт 3000
lsof -i :3000  # macOS/Linux
netstat -ano | findstr :3000  # Windows

# Измените порт в docker-compose.yml
```

### Проблема: Firewall блокирует

**Решение:**
- macOS: Разрешите Node.js в настройках Firewall
- Linux: `sudo ufw allow 3000/tcp`
- Windows: Добавьте правило в брандмауэре

---

## 📞 Быстрая справка

**Локальный доступ:** `http://localhost:3000`  
**Доступ из сети:** `http://ВАШ_IP:3000`  
**Проверка IP:** `ipconfig getifaddr en0` (macOS)  
**Проверка контейнера:** `docker-compose ps`  
**Логи:** `docker-compose logs -f`

